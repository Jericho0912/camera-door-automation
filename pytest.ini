[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    defect: documents a confirmed defect; the assertion describes the CORRECT
            behaviour and is expected to fail until the defect is fixed.
filterwarnings =
    ignore::DeprecationWarning
